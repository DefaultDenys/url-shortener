[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"

# Mount a volume at /data in the Railway dashboard, then set:
# DATABASE_URL=sqlite:///data/url_shortener.db?mode=rwc